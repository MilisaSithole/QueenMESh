// QueenMESh — Phase 0 scaffolding.
//
// Nothing here is game logic yet. The only job of this file is to prove the
// whole pipeline works end to end: HTML loads, CSS applies, JS executes, and
// the version you're looking at is the one you just pushed (not a cached one).
//
// Phase 0 deploy check: bump BUILD_MARKER, push, hard-refresh on desktop and on
// the phone. If the new string shows up on both, the deploy loop is trustworthy
// and every later phase is one commit away from being testable on a real URL.

const BUILD_MARKER = 'build 001';

document.getElementById('build-marker').textContent = BUILD_MARKER;
